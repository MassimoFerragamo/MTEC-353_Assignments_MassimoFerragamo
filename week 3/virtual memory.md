//think about letters 


// real world address is analog to computer address
// each address exactly matches 1 place
// this can be applied to computer memory
// the you create a variable you create a memory space and every variable has a specific memory address 

/* 
every variable 
*/

why am I typing in comments on an MD file hahaha

the fathest you go the further from the CPU and the slow it is to process
CPU 
Cache
RAM 
Hard Disk 

OS manages memory for you. 

virtual memory 

stack works with first in first out, like trays at caffetteria 
local variables are stored in stack and are stacked, 
when the function is done it destroys all of the local variables 

this influences pointers, as if you call the pointer of a variable outside of it's scope it will be nil 

I think I understand stack but I need to look at it again 

Heap 

be careful of heap 
you need to tell C when you are done using heap and destroy the memory, memory leak. 

you need to clear your own heap. 
